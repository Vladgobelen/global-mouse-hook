{
  "name": "global-mouse-hook",
  "version": "1.0.0",
  "description": "Global keyboard hook for PTT (standalone binary)",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "build:debug": "cargo build",
    "build:win64": "cargo build --release",
    "format": "cargo fmt",
    "lint": "cargo clippy"
  },
  "license": "MIT"
}